{
  "type": "object",
  "additionalProperties": false,
  "required": ["status", "summary", "blockers"],
  "properties": {
    "status": { "type": "string", "enum": ["approved", "rejected"] },
    "summary": { "type": "string" },
    "blockers": {
      "type": "array",
      "items": { "type": "string" }
    }
  }
}
